repositories {
    maven {
        url "url_place_holder"
        allowInsecureProtocol = true
    }
}

dependencies {
    //noinspection GradleDependency
	implementation ('com.regula.documentreader:api:version_place_holder'){
        transitive = true
    }
}
